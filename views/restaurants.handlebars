<section>
    <h2></h2>
    {{#each restaurants}} <!-- for each restaurants, create this html -->
        <article class="clearfix item-container block">
            <h2><span>{{name}}</span></h2>
            <img class="item-img" src="{{image}}" />
            <a class="btn" href="/restaurants/{{id}}">click here</a>
        </article>
    {{/each}}
</section>